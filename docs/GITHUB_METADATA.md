# GitHub metadata

Suggested description: Standard-library Python text renderer and protocol test bench for A3UI surfaces.

Suggested topics: `python`, `cli`, `a3ui`, `conformance`, `stdlib`.

These are manual GitHub settings; no remote metadata is changed by this repository.